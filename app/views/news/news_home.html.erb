
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h2 class="display-5">Noticias</h2>
    <p class="lead">

      <div class="card-columns">
        <% @noticias.each do |noticia| %>
      
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><%=noticia.title.truncate_words(10)%></h5>
                    <p class="card-text"><%=noticia.content.truncate_words(15)%></p>
                    <p class="card-text"><small class="text-muted"><%=noticia.created_at.to_s(:custom_datetime)%></small></p>
                    <a href="/news" class="btn btn-dark">Leer Más..</a>
                </div>
            </div>
        <% end %>
      </div>
    
    </p>
  </div>
</div>
