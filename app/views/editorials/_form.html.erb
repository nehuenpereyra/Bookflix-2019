<%=form_for @editorial do |f| %>
  <% if @editorial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@editorial.errors.count, "error") %></h2>
      No se pudo guardar la editorial por:
      <ul>
        <% @editorial.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :nombre %><br />
    <%= f.text_field(:name, {:class => 'form-control', :required => true})%>
  </p>
  <%= f.submit('Cargar editorial',{:class => 'btn btn-primary'}) %>
</p>
<% end %>
